'use strict';

function Metrics(opts) {
    this._startTime = process.hrTime();
    this._method = opts.method;
    this._url = opts.url;
}

module.exports = Metrics;

Metrics.prototype.start = function start(opts) {

}
